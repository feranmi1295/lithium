# 🔋 Lithium

Decentralized compute protocol. Idle computers become global infrastructure.

- `agent/` — Node Agent (C)
- `scheduler/` — Network Scheduler (Erlang/OTP)
- `terminal/` — CLI Interface (C)

lithium.network
